name: Building New Kernel

on: [push]

env:
  GIT_SECRETB: ${{ secrets.GIT_SECRETB }}
  GIT_SECRET: ${{ secrets.GIT_SECRET }}
  BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
  GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
  ZIP_PASS: ${{ secrets.ZIP_PASS }}
  TZ: Asia/Jakarta

jobs:
  build-lancelot-r-oss-stock:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: initialize
        run: |
          chmod +x misc/initialize.sh
          sudo bash misc/initialize.sh

      - name: Set Swap Space
        run: |
          chmod +x misc/setswap.sh
          sudo bash misc/setswap.sh 14

      - name: Compile Kernel
        run: |
          chmod +x maing.sh
          bash maing.sh 'lancelot-r-oss-stock'

  build-lancelot-r-oss-almk:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: initialize
        run: |
          chmod +x misc/initialize.sh
          sudo bash misc/initialize.sh

      - name: Set Swap Space
        run: |
          chmod +x misc/setswap.sh
          sudo bash misc/setswap.sh 14

      - name: Compile Kernel
        run: |
          chmod +x maing.sh
          bash maing.sh 'lancelot-r-oss-almk'

  build-lancelot-r-oss-slmk:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: initialize
        run: |
          chmod +x misc/initialize.sh
          sudo bash misc/initialize.sh

      - name: Set Swap Space
        run: |
          chmod +x misc/setswap.sh
          sudo bash misc/setswap.sh 14

      - name: Compile Kernel
        run: |
          chmod +x maing.sh
          bash maing.sh 'lancelot-r-oss-slmk'

  build-lancelot-r-oss-test-gcc:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: initialize
        run: |
          chmod +x misc/initialize.sh
          sudo bash misc/initialize.sh

      - name: Set Swap Space
        run: |
          chmod +x misc/setswap.sh
          sudo bash misc/setswap.sh 14

      - name: Compile Kernel
        run: |
          chmod +x maing.sh
          bash maing.sh 'lancelot-r-oss-gcc'

  build-merlin-r-oss-stock:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: initialize
        run: |
          chmod +x misc/initialize.sh
          sudo bash misc/initialize.sh

      - name: Set Swap Space
        run: |
          chmod +x misc/setswap.sh
          sudo bash misc/setswap.sh 14

      - name: Compile Kernel
        run: |
          chmod +x maing.sh
          bash maing.sh 'merlin-r-oss-stock'

  build-merlin-r-oss-almk:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: initialize
        run: |
          chmod +x misc/initialize.sh
          sudo bash misc/initialize.sh

      - name: Set Swap Space
        run: |
          chmod +x misc/setswap.sh
          sudo bash misc/setswap.sh 14

      - name: Compile Kernel
        run: |
          chmod +x maing.sh
          bash maing.sh 'merlin-r-oss-almk'

  build-merlin-r-oss-slmk:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: initialize
        run: |
          chmod +x misc/initialize.sh
          sudo bash misc/initialize.sh

      - name: Set Swap Space
        run: |
          chmod +x misc/setswap.sh
          sudo bash misc/setswap.sh 14

      - name: Compile Kernel
        run: |
          chmod +x maing.sh
          bash maing.sh 'merlin-r-oss-slmk'

  build-merlin-r-oss-gcc:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: initialize
        run: |
          chmod +x misc/initialize.sh
          sudo bash misc/initialize.sh

      - name: Set Swap Space
        run: |
          chmod +x misc/setswap.sh
          sudo bash misc/setswap.sh 14

      - name: Compile Kernel
        run: |
          chmod +x maing.sh
          bash maing.sh 'merlin-r-oss-gcc'

